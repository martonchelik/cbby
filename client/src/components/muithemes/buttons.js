import {createTheme} from "@mui/material/styles";


export const buttons = createTheme({
    palette: {
        primary: {
            main: "#4f79ff",
            contrastText: "#fff",
        },
        background: {
            default: "#394764"
        }
    }
});